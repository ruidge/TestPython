# coding=utf-8
'''
Created on 2014-9-14

@author: ruidge
'''
# 形参前面*表示序列**表示字典
def f1(x, y):
    print "%s -- %s" % (x, y)
f1(1, 2)
f1(*(1, 2))  # 长度为2的元组作为一一个参数传进去
f1(*{2, 3})  # 长度为2的列表作为一一个参数传进去
# f1(*'abc') #长度为2的序列作为一一个参数传进去,长度不匹配的时候报错

m = {"y" : 'abc', 'x' : 100}  # 长度为2的字典,乱序的只要key的值与形参匹配就可以
f1(**m)

print '-----------'

# 将传入的参数存入序列或者元组中
def f2(*a, **b):
    print a,
    print b
f2("a", 2, 3, 4, 5, "f")

f2("a", 2, 3, 4, 5, "f", x='xx', y='yy')


